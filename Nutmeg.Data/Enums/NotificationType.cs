namespace Nutmeg.Data
{
	// NOTE: Make sure this stays in sync with the client-side enum

	public enum NotificationType : short
	{
		NewDataAvailable = 0,
		NewPlaniswareProject = 1, 
		NewMicrosoftProject = 2,
	}
}
